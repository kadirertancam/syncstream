# SyncStream Backend Development Dockerfile
# ==========================================

FROM node:20-alpine

WORKDIR /app

# Install dependencies (including devDependencies for nodemon)
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

# Expose port
EXPOSE 4001

# Start with nodemon for hot reload
CMD ["npm", "run", "dev"]
